metadata:
  technique: zero_shot
  version: "1.0"
  created: "2026-07-08"
  description: >
    Baseline technique. Instruction only, no examples, no reasoning.
    All other techniques are compared against this baseline.

tasks:
  sentiment:
    template: |
      Classify the sentiment of the following sentence.
      Choose exactly one label: Positive, Negative, or Neutral.

      Sentence: {input_text}
      Sentiment:
    expected_labels: ["Positive", "Negative", "Neutral"]

  ner:
    template: |
      Identify all named entities in the following sentence.
      For each entity found, write it on a new line in this format: ENTITY | TYPE
      Types are: PERSON, ORGANIZATION, LOCATION, OTHER
      If no entities are found, write: NONE

      Sentence: {input_text}
      Entities:
    expected_labels: ["PERSON", "ORGANIZATION", "LOCATION", "OTHER", "O"]

  summarisation:
    template: |
      Write a one-sentence summary of the following article.
      Your summary must be concise and capture the main point only.

      Article: {input_text}
      Summary:
    expected_labels: []

  qa:
    template: |
      Answer the following question in as few words as possible.
      Give only the answer, no explanation.

      Question: {input_text}
      Answer:
    expected_labels: []

  paraphrase:
    template: |
      Do the following two sentences express the same meaning?
      Choose exactly one label: Paraphrase or Not Paraphrase.

      Sentence 1: {input_text}
      Sentence 2: {input_text_2}
      Label:
    expected_labels: ["Paraphrase", "Not Paraphrase"]